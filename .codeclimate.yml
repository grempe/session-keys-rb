---
engines:
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 30
  fixme:
    enabled: true
  rubocop:
    enabled: true
ratings:
  paths:
  - lib/**
exclude_paths:
- test/
